import styled from 'styled-components';

const Title = styled.h1`
  color: black;
`;

export default function MyComponent() {
  return (
    <div>
      <Title>Next.js Page with Styled Components</Title>
    </div>
  );
}
